define([
    'jquery',
], function($) {

    let site_methods = {};

    site_methods.backlightNavBar = function () {
        let pathname = window.location.pathname;
    }

    return site_methods;
});